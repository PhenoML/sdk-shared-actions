// Synthetic nested-type fixture: referenced by AgentChatRequest.categories
// to exercise list-of-object schema recursion in the TS extractor.

export interface Tag {
    name: string;
    color?: string;
}
